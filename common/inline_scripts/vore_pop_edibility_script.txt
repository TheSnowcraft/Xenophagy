# Sets weights for which pops get eaten by monthly nomming event.
weights = {
    base = 50
	# More Likely to be Eaten
    modifier = {
        factor = 5
        species = { has_citizenship_type = { country = root type = citizenship_vore_fodder } }
    }
    modifier = {
        factor = 10
        species = { has_trait = trait_vore_prey }
    }
    modifier = {
        factor = 10
        species = { has_trait = trait_vore_prey_artifical }
    }
    modifier = {
        factor = 4
        species = { has_trait = trait_docile }
    }
    modifier = {
        factor = 4
        species = { has_trait = trait_weak }
    }
    modifier = {
        factor = 4
        species = { has_trait = trait_rapid_breeders }
    }
    modifier = {
        factor = 4
        species = { has_trait = trait_delicious }
    }
    modifier = {
        factor = 4
        species = { has_trait = trait_nerve_stapled }
    }
    modifier = {
        factor = 4
        species = { has_trait = trait_fertile }
    }
    modifier = {
        factor = 4
        species = { has_trait = trait_presapient_docile_livestock }
    }
	# Less Likely to be Eaten
	modifier = {
        factor = -10
        species = { has_trait = trait_vore_pred }
    }
	modifier = {
        factor = -4
        species = { has_trait = trait_strong }
    }
	modifier = {
        factor = -8
        species = { has_trait = trait_very_strong }
    }
}
